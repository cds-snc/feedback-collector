{% extends "base.njk" %}

{% block content %}

    <h1>{{ __('send.title') }}</h1>

    <div>
        <p>
            {{ __('send.intro') }}
        </p>

        <form method="post">
            <input type="hidden" name="_csrf" value="{{ csrfToken }}">
            
            {{ textInput('form.firstname', 'form.firstname.desc') }}
            {{ formButtons() }}
        </form>
    </div>

{% endblock %}
